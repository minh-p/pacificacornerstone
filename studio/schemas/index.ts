import blockContent from './blockContent'
import category from './category'
import meetingNote from './meetingNote'
import author from './author'

export const schemaTypes = [meetingNote, author, category, blockContent]
